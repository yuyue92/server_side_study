## 关于Windows的.bat文件原理问题

windows环境下.py .bat文件的本质区别：.py需要关联到python解释器才能执行， .bat 文件是windows原生支持的批处理脚本，可以直接被命令提示符解释执行；双击.bat 文件时，windows会隐式调用cmd.exe来逐行执行里面的命令。

关键点在于，.bat文件实际上是在帮用户隐藏那些繁琐的技术细节。它把"启动命令提示符-激活Python环境-定位脚本路径-执行脚本"这一系列操作打包成了一个简单的双击动作。对于那些不熟悉命令行的用户来说，这大大降低了使用门槛。
